# Contributing

Certisphere changes must be production ready before merge.

Every feature must include:

- Acceptance criteria.
- Unit tests.
- Integration tests where external boundaries are crossed.
- API documentation and OpenAPI metadata for every endpoint.
- Security, logging, error handling, and audit trail coverage.
- Documentation updates.

Merge requests must pass the GitLab CI pipeline and receive code owner approval.
